/**
 * Migrate a single Macro document to incorporate latest data model changes.
 *
 * @returns {object}  The updateData to apply
 */
export default function migrateMacroData() {
  const updateData = {};

  return updateData;
}
